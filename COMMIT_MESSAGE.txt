feat: Switch to BACPAC export and add local database support

Change extraction method from DACPAC to BACPAC export and add comprehensive
local database connection string examples.

Changes:
- Switch from Extract() to ExportBacpac() method (exports schema + data)
- Remove using statement for DacServices (doesn't implement IDisposable)
- Update target framework from 4.6.2 to 4.8
- Add ConnectionStringExamples class with local database examples
- Update documentation with local SQL Server connection string examples
- Update ExampleConsoleApp with local database examples
- Fix compilation errors and ensure code builds successfully

Technical changes:
- DatabaseSchemaExtractor now uses ExportBacpac instead of Extract
- Supports both local SQL Server and Azure SQL Database
- Connection string examples for LocalDB, named instances, and Azure SQL
- All projects updated to .NET Framework 4.8

Breaking changes:
- Method now exports BACPAC (includes data) instead of DACPAC (schema only)
- Output file extension should be .bacpac instead of .dacpac
