Fix FilesController constructor error and add extract endpoint

- Fix FilesController constructor path resolution
  - Handle empty AppDomain.CurrentDomain.BaseDirectory
  - Add fallback to HttpRuntime.AppDomainAppPath for web applications
  - Add fallback to Environment.CurrentDirectory as last resort
  - Improve error handling for directory creation
  - Change default path to App_Data/Files (standard for web apps)
  - Prevents "Path cannot be empty" exception on API startup

- Add POST /api/files/extract endpoint
  - Extracts database schema and automatically saves to configured path
  - Uses DacpacFilesPath from Web.config if configured
  - Falls back to App_Data/Files if not configured
  - Auto-generates filename from database name if not provided
  - Creates directory automatically if it doesn't exist
  - Returns file path, size, and download URL in response
  - Supports all ExtractOptions (extractTableData, ignoreExtendedProperties, etc.)

- Update FileDownloadApi README
  - Add documentation for new extract endpoint
  - Include PowerShell usage examples
  - Update endpoint numbering

The extract endpoint enables automatic file saving to the configured path,
making extracted files immediately available for download via the API.
