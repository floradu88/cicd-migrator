Add PowerShell build scripts and fix compilation issues

- Add PowerShell scripts for solution management
  - restore.ps1: Restores NuGet packages for all projects
    * Auto-detects MSBuild, NuGet.exe, or dotnet CLI
    * Supports all projects in solution
  - build.ps1: Builds entire solution with dependency ordering
    * Supports Debug and Release configurations
    * Optional clean before build
    * Shows build output locations
  - run.ps1: Runs FileDownloadApi using IIS Express
    * Verifies API is built before running
    * Auto-detects IIS Express
    * Port availability checking
    * API connection testing on startup
    * Optional browser opening

- Add comprehensive documentation
  - scripts/README.md: Usage guide for all scripts
  - scripts/TEST_RESULTS.md: Test results and verification
  - FileDownloadApi/RUN_API.md: Detailed API run instructions
  - FileDownloadApi/VERIFICATION_SUMMARY.md: API verification checklist

- Fix compilation issues
  - Fix nullable bool handling in McpServer/Program.cs
    * Correct ExtractSchema method call with ExtractOptions
    * Fix validateConnection parameter handling
  - Update Web.config framework version from 4.6.2 to 4.8
    * Matches project target framework
    * Update both Web.config and FileDownloadApi.dll.config

- Fix script encoding issues
  - Replace Unicode characters (✓, ✗, →, ⚠) with ASCII equivalents
  - Ensures PowerShell compatibility across all environments

All scripts tested and verified working. Solution builds successfully.
